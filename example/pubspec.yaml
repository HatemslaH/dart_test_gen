name: example
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.2

dependencies:

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
