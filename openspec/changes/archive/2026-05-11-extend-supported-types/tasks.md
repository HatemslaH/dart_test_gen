## 1. Документация и фикстура

- [x] 1.1 Обновить `README.md`: блок «Возможности» (как описываются пользовательские типы) и пункт TODO 4 в соответствии с текущим поведением парсера и снимка
- [x] 1.2 Добавить `lib/usecases/type_extensions_showcase/` с основным классом и вспомогательными типами в отдельных файлах; методы с `Set` / `Iterable` как целевая регрессия для пункта 4

## 2. Парсер и граничные литералы

- [x] 2.1 Расширить `_paramFor` / `ParamType` для `Set<int>`, `Iterable<int>`, `List<String>` (и nullable/optional по тем же правилам, что у существующих типов)
- [x] 2.2 Добавить наборы граничных литералов в `test_generator.dart` с контролем размера комбинаторики

## 3. Снимок и литералы ожиданий

- [x] 3.1 Согласовать `snapshotValue` / `dartLiteralFromJson` для новых возвращаемых коллекций (`List<String>`, `Set<int>` и т.д. по дизайну)
- [x] 3.2 Прогнать `dart run bin/generate.dart lib/usecases/type_extensions_showcase/type_extensions_showcase.dart`, убедиться что для методов с коллекциями появляются непустые кейсы; добавить/обновить `test/usecases/...` при необходимости

## 4. Верификация

- [x] 4.1 `dart analyze` и `dart test` без регрессий
